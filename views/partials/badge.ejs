<%
const colorMap = {
  green: 'border',
  red: 'border',
};
const styleMap = {
  green: 'background:#e8fdf0;color:#15803d;border-color:#86efac',
  red: 'background:#fff0f0;color:#dc2626;border-color:#fca5a5',
};
const styleVal = styleMap[color] || styleMap.green;
%>
<span class="inline-block px-2 py-1 text-xs font-semibold rounded-full border" style="<%= styleVal %>">
  <%= label %>
</span>
